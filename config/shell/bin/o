#!/usr/bin/env bash
set -e
set -u
set -o pipefail

target="${1:-.}"
if [[ $OSTYPE == darwin* ]]; then
  exec open "$target"
elif [[ $OSTYPE == linux* ]]; then
  exec xdg-open "$target"
else
  exit 1
fi
