#!/usr/bin/env bash

set -euo pipefail
if ! command -v lazydocker >/dev/null 2>&1; then
  echo "lazydocker not found. Preview: mise run mise-sync; apply: mise run mise-sync -- --apply" >&2
  exit 1
fi

exec lazydocker "$@"
