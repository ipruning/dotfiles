#!/usr/bin/env bash

set -euo pipefail
target="${1:-.}"
if [[ $OSTYPE == darwin* ]]; then
  exec open "$target"
elif [[ $OSTYPE == linux* ]]; then
  exec xdg-open "$target"
else
  echo "o: unsupported OS: ${OSTYPE:-unknown}" >&2
  exit 1
fi
